config:
  target: "http://localhost:3000"
  phases:
    - duration: 20
      arrivalRate: 10

scenarios:
  - name: Test login carga baja 
    flow:
      - post:
          url: "/auth/login"
          json:
            nombre_usuario: "Instructor"
            password: "admin123"