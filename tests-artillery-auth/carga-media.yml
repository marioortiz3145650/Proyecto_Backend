config:
  target: "http://localhost:3000"
  phases:
    - duration: 20
      arrivalRate: 80

scenarios:
  - name: Test login carga media
    flow:
      - post:
          url: "/auth/login"
          json:
            nombre_usuario: "Instructor"
            password: "admin123"